# Copy to .env and adjust.
VITE_WALLET_BACKEND_URL=http://localhost:8080
# Must match wallet-backend's own BASE_CHAIN_ID (defaults to Base Sepolia,
# matching that service's own forgotten-env-var-safe default).
VITE_CHAIN_ID=84532
